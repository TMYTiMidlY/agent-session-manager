# 凭据 (S3 AK/SK + restic 密码)。**永远不要 track**。
secrets.env

# backup.sh 的运行时追加日志；每次跑 backup 都会追加，进 git 没意义。
backup.log

node_modules/
dist/
coverage/
.session-recall/

# Generated by packages/html/scripts/gen-assets.mjs at build time (inlined
# lucide icons + KaTeX css). Large and reproducible, so not tracked.
packages/html/src/assets.generated.ts
